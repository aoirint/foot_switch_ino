# foot_switch_ino

```shell
arduino-cli lib install HID-Project

arduino-cli board list

arduino-cli compile --fqbn "arduino:avr:leonardo"

arduino-cli upload --verbose --port "COM4" --fqbn "arduino:avr:leonardo"
```
